anthropic>=0.69.0
httpx>=0.27.0
python-dotenv>=1.0.1
rich>=13.7.0

# Phase 3 (browser). Install browsers with: python -m playwright install chromium
playwright>=1.47.0

# Dev / test only.
pytest>=8.0.0
