name: Lint Python

on:
  push:
    paths:
      - "**.py"
      - ".github/workflows/python.yml"
  pull_request:
    paths:
      - "**.py"
      - ".github/workflows/python.yml"

# Least privilege: linting only needs to read the checkout.
permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      # actions/checkout v5.1.0, pinned to its commit SHA
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09

      # Python ships preinstalled on GitHub's ubuntu-latest runners, so no
      # setup action is needed — same reasoning as the ShellCheck workflow.
      - name: Syntax check
        run: |
          python3 --version
          python3 -m compileall -q launchwatch

      # Pinned: an unpinned install makes this gate non-reproducible, since a
      # new Ruff release can add rules to the default set and turn the build red
      # without the code changing. Bump deliberately.
      - name: Run Ruff
        run: |
          python3 -m pip install --quiet ruff==0.16.1
          python3 -m ruff --version
          python3 -m ruff check .
